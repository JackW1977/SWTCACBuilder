/**
 * main.ts — front-end entry point for the Test Case Toolbox.
 *
 * Currently a stub.  As the front-end is extracted from templates/index.html
 * (inline CSS → style.css, inline JS → modules here), this file will grow into
 * the primary bundle entry point compiled by Vite into static/dist/main.js.
 *
 * Dev workflow
 * ------------
 *   1. Start Flask:   launch.bat            (serves the app on port 5000)
 *   2. Start Vite:    npm run dev           (proxies everything to Flask on port 5173)
 *   3. Open preview:  http://localhost:5173 (Claude Code preview window)
 */

// Placeholder — no-op until JS is migrated out of templates/index.html
export {};
