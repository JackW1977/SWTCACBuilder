# Node / Vite
node_modules/
package-lock.json
static/dist/

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd

# Virtual environment (created by deploy.bat)
venv/

# Persisted config — both committed; glean_config.json has URL only (no API key)
# Re-enter the API key via the Setup tab on first run on a new machine.

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Temp / test artefacts
_test_*.py
*.tmp

# Claude Code tooling (local IDE config)
.claude/

# Reference documents — internal/confidential, managed locally
references/*.docx
references/*.doc
references/*.pdf
references/*.txt
